dependencies {
    api project(":threedollar-external")
    api project(":threedollar-domain")
    api project(":threedollar-redis")

    // Jackson
    implementation("org.springframework.boot:spring-boot-starter-json")
}

bootJar { enabled = false }
jar { enabled = true }
